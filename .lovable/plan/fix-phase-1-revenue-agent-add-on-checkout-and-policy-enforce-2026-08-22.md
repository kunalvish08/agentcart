Fix Phase 1 Revenue Agent add-on checkout and policy enforcement.

1. Add-on pricing:

- When buyer accepts an add-on/cross-sell, its server-issued quote must be added to the order.

- Final order total must equal base product + accepted add-ons − applicable discounts.

- Never calculate the final amount from client-supplied prices.

- Recalculate/validate the complete order total server-side before checkout.

- Each accepted add-on must appear in the order with quantity, server price and quote/reference.

- Revenue event must record the incremental revenue generated by the add-on.

2. Policy enforcement:

- If upsell/cross-sell is OFF, get_eligible_related_products must return no recommendations.

- Add-on APIs/actions must reject attempts to add a blocked recommendation.

- The AI must not bypass this restriction by directly requesting the product.

- Log blocked recommendation attempts server-side.

3. Preserve:

- Existing catalog, quote, checkout, approval and Razorpay flows.

- Existing UI/theme.

- Do not modify navigation-menu.tsx.

- Do not modify unrelated components or backend logic.

Test:

Base ₹55,000 + USB-C Hub ₹1,299 = ₹56,299 before discounts.

The checkout/order must use the server-authoritative ₹56,299 total.